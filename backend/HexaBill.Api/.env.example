# =============================================================================
# HexaBill Backend - LOCAL .env (copy this to .env and fill in real values)
# =============================================================================
# 1. Copy: cp .env.example .env   (or copy contents into .env)
# 2. Replace all YOUR_xxx placeholders with your real values
# 3. .env is GITIGNORED - it will NEVER be pushed. Safe for passwords.
# =============================================================================

# --- PostgreSQL: Internal URL (use when backend runs on Render) ---
DATABASE_URL=postgresql://YOUR_USER:YOUR_PASSWORD@YOUR_INTERNAL_HOST/YOUR_DATABASE

# --- PostgreSQL: External URL (use when connecting from your PC / local dev) ---
# DATABASE_URL_EXTERNAL=postgresql://YOUR_USER:YOUR_PASSWORD@YOUR_EXTERNAL_HOST/YOUR_DATABASE

# --- PostgreSQL: separate fields (optional reference) ---
# DB_HOST_INTERNAL=YOUR_INTERNAL_HOST
# DB_HOST_EXTERNAL=YOUR_EXTERNAL_HOST
# DB_PORT=5432
# DB_NAME=YOUR_DATABASE
# DB_USER=YOUR_USER
# DB_PASSWORD=YOUR_PASSWORD

# --- PSQL command (paste into terminal to connect from your PC) ---
# PGPASSWORD=YOUR_PASSWORD psql -h YOUR_EXTERNAL_HOST -U YOUR_USER YOUR_DATABASE

# --- App ---
ASPNETCORE_ENVIRONMENT=Development
PORT=5000

# --- CORS (comma-separated). For Render: include your frontend URLs (Vercel + custom domain) ---
# Production: set in Render Dashboard → Environment. Any *.vercel.app is also allowed by code.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://www.hexabill.company,https://hexabill.company,https://hexabill.vercel.app

# --- JWT (generate: openssl rand -base64 48) ---
JwtSettings__SecretKey=YOUR_LONG_RANDOM_JWT_SECRET
JwtSettings__Issuer=HexaBill.Api
JwtSettings__Audience=HexaBill.Api

# --- Render MCP / API (https://dashboard.render.com → Account → API Keys) ---
# RENDER_API_KEY=your-render-api-key

# --- Vercel (Vercel Dashboard → Settings → Tokens; for CLI/MCP deploys) ---
# Add your token to .env locally. NEVER commit .env.
# VERCEL_TOKEN=your_token
